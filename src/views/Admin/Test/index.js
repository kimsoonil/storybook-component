import React, { useEffect, useMemo, useRef, useState } from 'react';
import Select, { useStateManager } from 'react-select';
import 'views/Admin/Test/test.scss';

const Test = ({}) => {
  return <div className="admin-test"></div>;
};

export default Test;
